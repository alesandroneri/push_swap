#include "../../push_swap.c"

/*static void del(int content)
{
    free(content);
}

void	ft_stackclear(t_stack **stack)
{
	t_stack	*temp;
	t_stack	*temp2;

	if (stack == NULL || *stack == NULL)
		return ;
	temp = *stack;
	while (temp != NULL)
	{
		temp2 = temp->next;
		del(temp->value);
		free(temp);
		temp = temp2;
	}
	*stack = NULL;
}*/